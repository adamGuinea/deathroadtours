import React from 'react';
import MessageList from '../containers/MessageList';

const MessageList = props => {
    return (
        <div className='row'>
            <MessageList />
        </div>
    );
};